import React from 'react';
import '../../styles/WeatherView.css';
import { CurrentWeather } from '../../types';
const WeatherView = (props: CurrentWeather) => {
  return <div className="WeatherView">WeaterView</div>;
};

export default WeatherView;
